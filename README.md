# my-neovim-config
My personal neovim config

I use two different colorschemes for my desktop and laptop config, to select which theme to use I set the **NVIM_MODE** environment variable in the `.zshrc` or `.bashrc` file:
```Bash
export NVIM_MODE="desktop"
```
